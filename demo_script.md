# デモ台本（SonarQube ウェビナー）

対象リポジトリ: `sonarqube_webinar_demo`
対象コード: `user_manager.py` / `test_user_manager.py` / `user_search.py`

---

## 0. 当日の流れ

| # | 内容 | 使うもの |
|---|------|----------|
| 1 | SonarQube IDE とは／その利点 | スライド |
| 2 | これから何を検知させるかの予告 | スライド |
| 3 | IDE デモ（7 件をその場で見せる） | VS Code |
| 4 | SQL インジェクションが「出ない」ことを見せる | VS Code |
| 5 | 他の Lint ツールと何が違うのか | スライド |
| 6 | では SQL インジェクションはどうするか | スライド |
| 7 | push → ダッシュボード（SQLi / カバレッジ / SBOM） | ブラウザ |

狙いは「手元で出るもの」と「サーバーでしか出ないもの」を体感で分けてもらうことです。

---

## 1. 事前準備チェックリスト

**解析方法（ここを間違えるとカバレッジが出ません）**

- [ ] SonarQube Cloud で **Automatic Analysis を無効**にし、**CI-based analysis** を選ぶ
      （Automatic Analysis はカバレッジレポートを取り込めない）
- [ ] GitHub のリポジトリ Secrets に `SONAR_TOKEN` を登録
- [ ] SBOM を見せる場合は、プロジェクトの **Advanced Security（SCA）を有効**にしておく

**IDE 側（最重要）**

- [ ] **`user_search.py` を main から外した状態で始める**

      サーバーが解析済みのコードに SQL インジェクションが含まれていると、
      その検出結果が手元の IDE 側にも同期されてしまい、「IDE では出ない」
      という山場が崩れます。ファイルは手元に残したまま、リポジトリの
      追跡対象からだけ外しておきます（対応済み。`git push` だけ必要）。

      ```bash
      git status          # user_search.py が ?? になっていること
      git push            # main から外したコミットを反映
      ```

      push 後、Cloud の再解析が終わったら VS Code のウィンドウを再読み込み。
      パネルから SQL インジェクションが消えていることを確認する。

- [ ] SonarQube パネルの件数が **7 件**であることを確認する
      （`user_manager.py` 4 件 + `test_user_manager.py` 3 件）
      （それ以外が出ていたら事前に潰しておく）

**ローカルでカバレッジを出す場合（CI に任せるなら不要）**

```bash
pip install -r requirements.txt
coverage run --source=. --omit="test_*.py" -m unittest discover
coverage xml
```

---

## 2. IDE デモ本編

### 導入（`user_manager.py` を開いた状態で）

> 「まだ何も実行していません。テストも走らせていません。
> ファイルを開いただけです。それでもう、この状態です。
>
> 入れるものは拡張機能 1 つだけ。準備するものはありません。
> 解析エンジンがエディタの中に入っているので、待ち時間もありません。
> そして、このコードは 1 バイトも外に出ていません。」

SonarQube パネルに 7 件出ているところを見せる。

---

### 【1】API キーのハードコード — `python:S6418`

> 「まず 7 行目。API キーをソースコードに直接書いています。
> コミットする前、レビューに出す前、**書いたその場で**分かる。
> GitHub に上げてから気づくのとは、直すコストが桁違いです。」

指摘をホバーして、ルールの説明と「なぜ危険か」が出るところを見せる。

---

### 【2】パスワードのハードコード — `python:S2068`

> 「次はパスワードです。
> パスワードは特に、一度コミットすると Git の履歴から消すのが非常に面倒です。
> **履歴に残る前に止める**、それができるのは手元だけです。」

---

### 【3】ZIP を無制限に展開している — `python:S5042`

> 「アップロードされた ZIP を `extractall()` で丸ごと展開しています。
> 展開後のサイズを一切確認していません。
>
> 数十 KB の ZIP が、展開すると数十 GB になるという細工が可能です。
> ディスクを埋め尽くしてサービスを止められます。Zip Bomb と呼ばれる手口です。
>
> 正しくは `ZipInfo.file_size` を先に合計して、上限を超えるものは展開せずに弾きます。」

---

### 【4】制御構文のネストが深すぎる — `python:S134`

> 「毛色の違うものも見てください。セキュリティではなく**保守性**です。
> try の中に if、その中にまた if …… と 5 段潜っています。
>
> **動きます。テストも通ります。** 今は誰も困っていません。
> でも半年後、条件を 1 つ足してくれと言われたとき、どこに手を入れるか即答できますか。
>
> セキュリティもバグも保守性も、**1 つの拡張機能がまとめて拾います**。
> ツールを 3 つ入れて 3 つのレポートを見る必要はありません。」

指摘をクリックして、ネストの各段がハイライトされるところを見せる（ここが効く）。

---

### 【5】いつも成功してしまう assert — `python:S5905`

`test_user_manager.py` を開く。

> 「テストコードも解析対象です。
>
> この assert、括弧が付いています。assert は関数ではないので、
> 括弧を付けると引数を囲んだことにならず、**タプルができます**。
> 空でないタプルは常に真なので、**この assert は絶対に失敗しません**。
>
> 厄介なのは、スキップされるわけではないことです。堂々と PASS します。緑になります。
> 『テストは 4 件とも通っています』という報告に、嘘は 1 つもありません。
> ただ、そのうち 1 件は何も守っていないだけです。」

実演すると効果的:
`user_manager.py` の `rank = "S"` を `rank = "Z"` に書き換えて
`python3 -m unittest test_user_manager -v` を実行 → **それでも通る**ところを見せる。

---

### 【5-2】例外ブロックの後ろに書いた assert — `python:S5915`

同じファイルを少し下へスクロール。

> 「もう 1 つ、PASS するのに守っていないテストです。
>
> `assertRaises` の中で、ファイルが無いときに例外が出ることを確認しています。
> そのあと、戻り値も確認しています。……ように見えます。
>
> 実際は、例外が出た瞬間に `with` を抜けます。**2 行目は一度も実行されません。**
> 証拠に、この assert は中身が間違っています。`"error"` なんて返しません。
> それでも誰も気づかない。実行されていないからです。」

直し方は「例外を出す行を `with` の最後の 1 行にする」。

---

### 【5-3】名前のせいで実行されないテスト — `python:S5899`

> 「最後はこれです。中身は正しく書けています。`assertEqual` も使っています。
>
> でも、名前が `check_` で始まっています。unittest は `test` で始まる
> メソッドしか実行しません。**このテストは一度も走っていません。**
>
> 失敗もしない、スキップとも出ない。実行件数が 1 件少ないだけです。」

ターミナルで `python3 -m unittest test_user_manager -v` を実行し、
**メソッドは 5 つあるのに `Ran 4 tests`** と出るところを見せる（ここが効く）。

> 「まとめると、今の 3 つは全部『PASS するのに守っていないテスト』です。
>
> - assert が必ず成功する
> - assert が実行されない
> - テストそのものが実行されない
>
> どれもテスト結果は緑です。テストを走らせても分かりません。
> **書いているその場で、コードを読んで指摘できる**のが静的解析の強みです。」

---

## 3. 【6】SQL インジェクション — 「出ない」ことを見せる

`user_search.py` を開く。

> 「では最後に、このファイルを見てください。
> 外部から来た値を受け取り、それを**文字列連結でそのまま SQL に**
> 埋め込んでいます。教科書どおりの SQL インジェクションです。
>
> ——ところが、**指摘が出ていません**。
>
> なぜか。ここまでの 7 件は、どれも『その場のコードの書き方』を見れば判定できました。
> SQL インジェクションだけは違います。必要なのは
> **『値がどこから来て、どこへ届くか』を追跡すること**、汚染解析（Taint Analysis）です。
>
> 出発点は `request.args.get()`、到達点は `cursor.execute()`。
> 今日は隣に並んでいますが、実際のプロジェクトではファイルも層もまたいで、
> 何段も関数を経由します。プロジェクト全体の全経路を追う解析は、
> キーストロークごとに走らせるものではありません。
>
> これは**サーバー側の仕事**です。」

→ ここでスライドに戻る（他の Lint との違い → サーバー解析の役割）

---

## 4. push → ダッシュボード

```bash
git add user_search.py
git commit -m "ユーザー検索機能を追加"
git push
```

GitHub Actions が走る → SonarQube Cloud のプロジェクトを開く。

### (1) SQL インジェクション — `python:S3649`

Issues（または Security）から `python:S3649` を開く。

> 「出ました。しかも『危険です』と言うだけではありません。
> **どこから入って、どこへ届いたのか**、経路が全部出ています。
> 手元では出なかったものが、push したらプロジェクト全体を横断して見つかる。
> これが役割分担です。**手元では書いたその瞬間に、push 後は全体を横断して。**」

source から sink までの経路が可視化される画面を見せる。

### (2) テストカバレッジ — 52.5%

Measures > Coverage を開く。

> 「カバレッジは 52.5% です。半分ほどしかテストが通っていません。
> ——ここで大事なのは、この数字ではありません。**中身**です。」

`user_manager.py` を開いて、行単位の赤／緑を見せる。

> 「赤い行が未テストです。
> ZIP の展開処理は、ファイルが無いときの例外しか確認していません。
> ランク A の分岐も通っていません。
>
> そしてランク B。**テストは書いてあるのに赤い。**
> さっき IDE で見た、名前が `check_` のあのテストです。
> 実行されていないテストは、カバレッジにもちゃんと『未テスト』として出てきます。
> そして `except ValueError` ——**エラー処理に誰もテストを書いていない**。
>
> 『正常系だけ書いて、異常系は後回し』。実プロジェクトで一番多い形が、
> そのまま行単位で見えています。次にどのテストを書くべきかは、もう決まりました。」

`user_search.py` も開く。

> 「そしてこちら、カバレッジ 0% です。
> **脆弱性があるファイルに限って、テストが 1 件もない。**
> これもよくある話です。」

### (3) SBOM / 依存関係

Dependency Risks（SBOM）を開く。

> 「最後に依存関係です。自分が書いたコードは今見たとおりですが、
> アプリの中身の大半は**他人が書いたコード**です。
> 何のライブラリを、どのバージョンで使っているか。既知の脆弱性はないか。
> SBOM としてエクスポートもできます（CycloneDX / SPDX）。
>
> 『自分のコード』と『持ち込んだコード』の両方を、同じダッシュボードで見られます。」

---

## 5. まとめの一言

> 「手元では、書いたその瞬間に、準備なしで、コードを外に出さずに。
> push 後は、プロジェクト全体を横断して、経路まで追って、依存関係まで含めて。
>
> どちらか一方ではなく、**同じ基準のまま、両方**。
> それが SonarQube です。」

---

## 6. 当日の注意メモ

- **`user_search.py` を main から外した状態で始める**（最重要）。
  push 済みだと SQLi の検出結果が IDE 側にも同期され、山場が崩れる
- **Automatic Analysis は無効**にしておく。有効だとカバレッジが空欄になる
- パネルの件数は 7 件。それ以外が出ていたら事前に潰す
- `rank = "S"` を `"Z"` に書き換えて戻すのを忘れない（【5】の実演をした場合）
- 【5-2】【5-3】は直さずに push する（直すとカバレッジの「B が赤い」話が使えなくなる）
- S5915 / S5899 が品質プロファイルで有効になっているか、事前に IDE で指摘が出ることを確認する
- ダッシュボードは解析完了まで 1〜2 分かかる。待ち時間にスライドの質疑を挟むと自然
- push 後しばらくすると、SQLi の指摘が VS Code 側にも出てくる。
  そのまま「サーバーが見つけた指摘が、開発者の手元にも返ってくる」という
  締めに使える（触れずに終わってもよい）
